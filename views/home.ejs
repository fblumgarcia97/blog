<%- include("partials/header") -%>
    <div>
        <h1>HOME</h1>
        <p><%=startingContent%></p>
        <%posts2.forEach(function(post) {%>
            <h1><%=post.name%></h1>
            <p class="limit"><%=post.content.substring(0,100)%>...<a href="/post/<%=post._id%>"> Read more</a></p>
        <%})%>
    </div>
<%- include("partials/footer") -%>